import pip

pip.main(["install", "art", "--user"])
pip.main(["install", "opencv-python", "--user"])
pip.main(["install", "webcolors", "--user"])
pip.main(["install", "getch", "--user"])
pip.main(["install", "moviepy", "--user"])